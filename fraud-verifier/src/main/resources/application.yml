server:
  port: 9081

spring:
  application:
    name: fraud-verifier
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
logging:
  level:
    org.springframework: info
management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - metrics
          - prometheus
