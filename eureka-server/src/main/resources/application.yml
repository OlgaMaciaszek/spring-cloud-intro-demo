server:
  port: 8761


eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
management:
  endpoints:
    web:
      exposure:
        include:
          - metrics
          - prometheus
spring:
  application:
    name: eureka-server
